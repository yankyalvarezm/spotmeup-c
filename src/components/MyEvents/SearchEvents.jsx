import React from "react";

const SearchEvents = () => {
  return (
    <input
      type="search"
      placeholder="Search events..."
      className="search-venues"
    />
  );
};

export default SearchEvents;
